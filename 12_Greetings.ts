/* Greetings: Start with array you used in Exercise 11, but instead of just 
printing each person's name, print a message to them. The text of each message
should be the same, but each message should be personalized with the person's
name. */



const friendsNames2 : string[] = ["Faizan", "Hassan", "Husnain", "Shah G"];

for (let i = 0; i < friendsNames2.length; i++){

    console.log(`Hi, ${friendsNames2[i]} How are you doing?`);
}

